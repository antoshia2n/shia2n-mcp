/**
 * shia2n-mcp の版番号は、このファイル 1 か所だけに置く。
 *
 * 2026-08-08 v0.44.0：新設。
 *   それまでは版番号が 2 か所にあった。index.ts の 0.43.0（道具の名乗り）と、
 *   diag.ts の 0.18.0（点検画面が返す値）で、数え方そのものが別だった。
 *   点検画面を開いた人は 0.18.0 を見るため、実物より 25 も古い番号を
 *   「今動いている版」と読んでしまう。
 *   置き場を 1 つにして、どちらもここを読む形にした。
 *
 * 版を上げるときは、この 1 行だけを書き換える。
 * 何を変えたかは index.ts の先頭の履歴に足す。
 */
export const APP_VERSION = "0.44.0";
